<%= form_for(@callcenter) do |f| %>
  <% if @callcenter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@callcenter.errors.count, "error") %> prohibited this callcenter from being saved:</h2>

      <ul>
      <% @callcenter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :people_id %><br>
    <%= f.text_field :people_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
